#!/bin/bash
BACKUP_NAME=$(date +'%d_%m_%Y')_${OPENSHIFT_GEAR_NAME}
mysqldump --password=${OPENSHIFT_MYSQL_DB_PASSWORD} -u ${OPENSHIFT_MYSQL_DB_USERNAME} -h ${OPENSHIFT_MYSQL_DB_HOST} -P ${OPENSHIFT_MYSQL_DB_PORT} ${OPENSHIFT_GEAR_NAME} > ${OPENSHIFT_REPO_DIR}/data/${BACKUP_NAME}.sql